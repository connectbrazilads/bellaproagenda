# Em producao, o frontend deve falar com a API pelo mesmo dominio via /api.
# VITE_API_URL pode continuar vazio se voce usar Nginx com proxy reverso.
VITE_API_URL=
VITE_WEBHOOK_SECRET=troque_por_um_segredo_igual_ao_backend
